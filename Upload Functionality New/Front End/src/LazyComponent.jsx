const LazyComponent = () => {
    return(
        <div>
            <h1>Lazy Loaded Component which only loads when the component is requested!</h1>
        </div>
    );
};
export default LazyComponent;